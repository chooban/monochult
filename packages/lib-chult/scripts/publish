#! /bin/bash

CURRENT="$(npm run --silent currentversion)"
DEPLOYED="$(npm show . version)"

if [ "$CURRENT" = "$DEPLOYED" ]; then
  echo "Versions match"
  npx release-it
else
  echo "Version manually bumped"
  npx release-it $CURRENT
fi
